"use client";

import "@tailwindplus/elements";

// Registers Tailwind Plus web components on the client so their behaviors (e.g., dropdowns) work.
export default function TailwindElementsLoader() {
  return null;
}
